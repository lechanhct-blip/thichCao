name: Build

concurrency: 
  group: "build"
  cancel-in-progress: true

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout builds
        uses: actions/checkout@v4
        with:
          ref: "builds"
          path: "builds"
      
      - name: Clean old builds
        run: rm -f $GITHUB_WORKSPACE/builds/*.cs3

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build Plugins and Generate JSON
        run: |
          chmod +x gradlew
          # 1. Biên dịch toàn bộ file .cs3
          ./gradlew makeAll --stacktrace

          # 2. Tạo file plugins.json
          ./gradlew makePluginsJson --stacktrace || true

          # 3. Tạo thư mục chứa output để đăng tải
          mkdir -p $GITHUB_WORKSPACE/builds

          # 4. Copy toàn bộ file .cs3 tìm thấy
          find . -name "*.cs3" -exec cp {} $GITHUB_WORKSPACE/builds \;

          # 5. Copy file plugins.json (nếu nằm trong thư mục build gốc hoặc subproject)
          find . -name "plugins.json" -exec cp {} $GITHUB_WORKSPACE/builds \;

      - name: Push builds
        run: |
          cd $GITHUB_WORKSPACE/builds
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add .
          git commit --amend -m "Build $GITHUB_SHA" || exit 0
          git push --force
